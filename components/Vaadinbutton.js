import * as React from "react";
import { StyleSheet, View } from "react-native";

const Vaadinbutton = () => {
  return <View style={styles.vaadinbutton} />;
};

const styles = StyleSheet.create({
  vaadinbutton: {
    width: 24,
    height: 24,
    overflow: "hidden",
  },
});

export default Vaadinbutton;
